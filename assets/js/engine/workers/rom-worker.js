self.onmessage=async e=>{const {id,op,buffer}=e.data||{};try{const bytes=new Uint8Array(buffer);if(op==='hash'){let c=0xffffffff;for(const b of bytes){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}const crc=((c^0xffffffff)>>>0).toString(16).padStart(8,'0').toUpperCase();let sha1='',sha256='';if(self.crypto?.subtle){const d1=await crypto.subtle.digest('SHA-1',bytes),d2=await crypto.subtle.digest('SHA-256',bytes);sha1=Array.from(new Uint8Array(d1),x=>x.toString(16).padStart(2,'0')).join('').toUpperCase();sha256=Array.from(new Uint8Array(d2),x=>x.toString(16).padStart(2,'0')).join('').toUpperCase()}postMessage({id,ok:true,result:{crc32:crc,sha1,sha256}},[]) } else postMessage({id,ok:false,error:'unsupported op'})}catch(err){postMessage({id,ok:false,error:String(err?.message||err)})}}
